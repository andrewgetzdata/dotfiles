# Copy this to config/.dotfiles_env and fill in your values
# .dotfiles_env is gitignored and never committed
export ANTHROPIC_API_KEY=""
export PERSONAL_GITHUB_TOKEN=""
export FIRECRAWL_API_KEY=""

# Granola MCP uses browser OAuth — no API key needed here.
# Authenticate on first use in Claude Code.
